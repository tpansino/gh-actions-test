name: "Delete"

on:
  workflow_dispatch:
  delete:

jobs:
  delete:
    name: Delete
    runs-on: ubuntu-20.04
    steps:
      - run: |
          slug="[$(echo "${{ github.event.ref }}" | sed -e 's#^refs/heads/##' | cut -c 1-50)]"
          echo SLUG=$slug
