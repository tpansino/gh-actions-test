name: "Test"

on:
  push:

jobs:
  test:
    name: "Test"
    runs-on: ubuntu-latest
    steps:
    - name: "Test Action"
      uses: ./.github/actions/action.yml@main
      with:
        subject: "Universe"

  test2:
    name: "Test2"
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: |
          echo "foo = '${{ needs.test.outputs.foo }}'"
